{
	"$schema": "node_modules/wrangler/config-schema.json",
	"main": ".open-next/worker.js",
	// Nombre real registrado en Cloudflare Workers Builds para este repo —
	// confirmado por el propio CI en un aviso de deploy ("the CI system
	// expected 'portfolio'"), no una suposición. Cualquier otro valor aquí
	// da igual lo que ponga el `name` de package.json.
	"name": "portfolio",
	"compatibility_date": "2026-08-20",
	"compatibility_flags": [
		"nodejs_compat",
		"global_fetch_strictly_public"
	],
	"observability": {
		"enabled": true
	},
	"assets": {
		"directory": ".open-next/assets",
		"binding": "ASSETS"
	},
	"services": [
		{
			// El binding de autorreferencia: el nombre del servicio tiene que
			// coincidir con el `name` de arriba.
			// ver https://opennext.js.org/cloudflare/caching
			"binding": "WORKER_SELF_REFERENCE",
			"service": "portfolio"
		}
	],
	"images": {
		// Optimización de imágenes de Cloudflare
		// ver https://opennext.js.org/cloudflare/howtos/image
		"binding": "IMAGES"
	},
	"kv_namespaces": [
		{
			// Historial real de /api/status: una muestra por comprobación,
			// para que el % de uptime del panel salga de datos que de verdad
			// pasaron, no de un número inventado. Sin esto no sobrevive a un
			// cold start del Worker — un Map en memoria se vacía en cada uno.
			"binding": "STATUS_HISTORY",
			"id": "24d0077456c14bfba2cdf102bf4c48e4",
			"preview_id": "c6cb6db52e83400897f878caa0dc5708"
		}
	]
}
